# Copy to .env and set your API keys

# Tavily (default)
TAVILY_API_KEY=

# Optional: SerpAPI (used when provider=serpapi)
SERPAPI_KEY=

# Optional: default provider override (tavily | serpapi)
# DEFAULT_PROVIDER=tavily
